spring:
  application:
    name: day14-api
  datasource:
    url: jdbc:h2:mem:day14;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
    username: sa
    password:
  sql:
    init:
      mode: never

server:
  port: 8080

logging:
  level:
    com.example.day14: INFO
